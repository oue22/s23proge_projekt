<!DOCTYPE html>
<html>
    <head>
        <title>Publiku lemmiku poolt hääletus</title>
        <meta charset="UTF-8">
        <link rel="stylesheet" type="text/css" href="/static/style.css">
    </head>
    <body>
        <h1>Lemmiku poolt hääletamine</h1>
		<h2>Osalejad kelle poolt saad hääletada:<h2>
		
		<form method="POST" action="{{ url_for('handle_button_click') }}">
			<ul>
				{% for osa in osaleja %}
				<li>{{ osa }}<button type="submit" id="{{ loop.index }}_button" name="vajutatud_nupp" value="{{ osa }}">Vajuta mind</button></li>
				{% endfor %}  
			</ul>
		</form>
    </body>
</html>